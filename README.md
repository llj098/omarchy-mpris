# Media Players

Minimal Omarchy bar widget that lists every MPRIS player and gives each one a single play/pause button.

It uses `Quickshell.Services.Mpris` directly and does not select, cache, merge, or redirect players.
